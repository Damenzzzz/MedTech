import 'server-only';
import {DebriefResultSchema,TrainingSessionSchema,type DebriefResult} from '@/domain/schemas';
import {getCaseRepository} from '@/repositories/index.server';
export async function scoreSession(raw:unknown):Promise<DebriefResult>{const session=TrainingSessionSchema.parse(raw);const item=await getCaseRepository().getGroundTruth(session.caseId);if(!item)throw new Error('Case not found');const types=new Set(session.actions.map(a=>a.type));const correct=session.finalDiagnosis===item.correctDiagnosis.code;const indicated=new Set(item.investigations.filter(x=>x.indicated).map(x=>x.id));const unnecessary=session.selectedInvestigations.filter(x=>!indicated.has(x));const categories={history:Math.min(100,session.revealedFactIds.length/item.hiddenFacts.length*100),examination:types.has('examination')?85:20,investigations:Math.max(20,100-unnecessary.length*25),differential:session.differentials.length?80:20,diagnosis:correct?100:20,management:session.managementNotes.length>20?80:30,communication:types.has('communication')?90:65,critical:item.urgency==='emergency'&&session.actions.length<2?30:90};const total=Math.round(Object.entries(categories).reduce((sum,[key,value])=>sum+value*item.scoringRubric[key as keyof typeof item.scoringRubric]/100,0));const missed=item.hiddenFacts.filter(f=>!session.revealedFactIds.includes(f.id));return DebriefResultSchema.parse({caseId:item.id,total,categories,missedQuestions:missed.map(f=>f.value.ru),foundRedFlags:item.hiddenFacts.filter(f=>f.critical&&session.revealedFactIds.includes(f.id)).map(f=>f.value.ru),missedRedFlags:missed.filter(f=>f.critical).map(f=>f.value.ru),investigationFeedback:unnecessary.length?[`Необоснованные исследования: ${unnecessary.join(', ')}`]:['Набор исследований соответствует учебной задаче'],criticalErrors:correct?[]:['Итоговый диагноз не совпал с эталонным'],strengths:[categories.history>=70?'Системный сбор анамнеза':'Сессия завершена с клиническим обоснованием'],recommendations:['В следующем случае раньше обозначьте красные флаги','Связывайте каждое исследование с клинической гипотезой'],timeline:session.actions,referencePlaceholders:[{title:'Клиническая рекомендация — источник будет подключён через RAG',status:'rag-pending'}],correctDiagnosis:`${item.correctDiagnosis.code} — ${item.correctDiagnosis.name.ru}`});}
