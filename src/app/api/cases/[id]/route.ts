import {StudentCaseDTOSchema} from '@/domain/schemas';import {getCaseRepository} from '@/repositories/index.server';export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const data=await getCaseRepository().getStudentCase(id);if(!data)return Response.json({error:'not_found'},{status:404});return Response.json(StudentCaseDTOSchema.parse(data))}
