import {notFound} from 'next/navigation';import {getCaseRepository} from '@/repositories/index.server';import {TrainingWorkspace} from '@/components/training/training-workspace';export default async function Training({params}:{params:Promise<{caseId:string}>}){const {caseId}=await params;const patient=await getCaseRepository().getStudentCase(caseId);if(!patient)notFound();return <TrainingWorkspace patient={patient}/>}
