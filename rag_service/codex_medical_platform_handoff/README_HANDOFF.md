# Codex Handoff

Copy this package into the root of the medical-platform repository.

Then:

```bash
cd /path/to/your-repository
codex
```

Paste the contents of `CODEX_START_PROMPT.md` as the first task.

Do not paste the whole ChatGPT conversation. The files in this package contain the useful decisions in a structured form that Codex can follow.
