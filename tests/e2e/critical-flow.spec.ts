import {test,expect} from '@playwright/test';
test('landing opens patient queue and API does not leak ground truth',async({page,request})=>{await page.goto('/ru');await expect(page.getByRole('heading',{level:1})).toBeVisible();await page.getByRole('link',{name:'Открыть приёмную'}).click();await expect(page).toHaveURL(/\/ru\/patients/);await expect(page.getByText('Синтетический случай').first()).toBeVisible();const response=await request.get('/api/cases');expect(response.ok()).toBeTruthy();const cases=await response.json() as Record<string,unknown>[];expect(cases).toHaveLength(15);expect(cases[0]).not.toHaveProperty('hiddenFacts');expect(cases[0]).not.toHaveProperty('correctDiagnosis');expect(cases[0]).not.toHaveProperty('scoringRubric')});
